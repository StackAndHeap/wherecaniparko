<?xml version="1.0"?>
<s:View xmlns:fx="http://ns.adobe.com/mxml/2009" xmlns:s="library://ns.adobe.com/flex/spark"
		title="Parkings">

	<fx:Script><![CDATA[
		import model.ParkingSpot;
		import model.Session;
		import model.SpotState;

		import mx.collections.ArrayCollection;
		import mx.core.FlexGlobals;
		import mx.events.FlexEvent;

		import spark.events.IndexChangeEvent;



		[Bindable]
		private var _parkings:ArrayCollection = createParkings();


		[Bindable]
		private var _buttons:ArrayCollection = new ArrayCollection(["Filter", "Instellingen"]);

		private function createParkings():ArrayCollection {
			var result:ArrayCollection = new ArrayCollection();
			result.addItem("test");

			result.addItem("test");
			result.addItem("test");
			result.addItem("test");
			result.addItem("test");
			result.addItem("test");
			return result;
		}

		private function list1_clickHandler(event:MouseEvent):void {
			trace("test");

			if (list.selectedItem is ParkingSpot) {
				var spot:ParkingSpot = list.selectedItem;

			}
		}

		private function list_changeHandler(event:IndexChangeEvent):void {
			trace("list change");
		}



		]]></fx:Script>

	<s:Label text="{Session.location}"/>


		<s:List id="list"
			        dataProvider="{Session.parkings}"
			        width="100%" height="100%"
			        labelField="name"
			        itemRenderer="view.renderer.ParkingItemRenderer"
			        click="list1_clickHandler(event)"
					change="list_changeHandler(event)">

			</s:List>

</s:View>
